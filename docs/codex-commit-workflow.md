# Codex Commit Workflow

- Review changes before commit.
- Run `npm run build` before commit.
- Verify the relevant page returns `200`.
- Never use `git add .` unless explicitly approved.
- Add only approved files.
- Use one commit per logical phase.
- Do not commit or push before explicit approval.
- After push, report the commit hash, branch, and final git status.
